PORT=3001
JWT_SECRET=leetflixv3CodeForSurvival2026
JWT_EXPIRES_IN=7d

# Firebase — OPTION A: inline JSON (for Railway / Render — paste the full service account JSON on one line)
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"...","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"...@....iam.gserviceaccount.com","client_id":"..."}

# Firebase — OPTION B: file path (for local dev — comment out FIREBASE_SERVICE_ACCOUNT above)
# FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json

# CORS — comma-separated list of allowed frontend origins
# Local dev:
FRONTEND_URL=http://localhost:3000
# Production (add your Vercel URL):
# FRONTEND_URL=https://leetflix.vercel.app,http://localhost:3000
